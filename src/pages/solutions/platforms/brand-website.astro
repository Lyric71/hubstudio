---
/**
 * Brand website · /solutions/platforms/brand-website
 *
 * Editorial layout adapted from /solutions/platforms/meta. Same print-shop
 * primitives (crop marks, calibration color bar, mono figure labels, dashed
 * rules) reframed for the brand's own company website: the five kinds of
 * content work we ship into a site, the AIGC mechanics for each, the
 * partnership model. Content is the authored part; the design and the code
 * are AI-assisted.
 *
 * Page excerpt (25 words): hubStudio writes brand website content across
 * five lanes: core pages, offerings, editorial, SEO and GEO copy, all built
 * from one brief and one editorial voice.
 *
 * Per repo standards: no em-dashes (U+2014), no decorative numerals inside
 * cards or repeated blocks, body copy at var(--type-body), hubStudio keeps
 * its canonical lowercase-h casing.
 */
import Layout from '../../../layouts/Layout.astro';
import Button from '../../../components/Button.astro';

const colourBar = [
  '#e94e1b', '#c73e10', '#1e3a8a', '#0e1f4e', '#0e1633',
  '#6b6b73', '#a8a299', '#d8d3cb', '#e5e1db', '#ffffff',
];

const stats = [
  { from: 0, to: 5, suffix: '', label: 'Lanes we ship', note: 'Core, offerings, editorial, SEO, GEO' },
  { from: 0, to: 50, suffix: '+', label: 'Pages from one brief', note: 'A full site plus the content library' },
  { from: 0, to: 48, suffix: 'h', label: 'Brief to first draft', note: 'On a single page or article' },
];

const samples = [
  {
    src: '/Images/brand-website-sample-homepage.jpg',
    kind: 'Core pages · Homepage',
    alt: 'A laptop screen filled with a company website homepage, a top navigation menu, a large headline, a short paragraph and a call-to-action button, a band of feature cards below.',
  },
  {
    src: '/Images/brand-website-sample-solution.jpg',
    kind: 'Offerings · Solution page',
    alt: 'A laptop screen showing a company website solution page, a headline, a block of explanatory text, a row of three feature points and a call-to-action button.',
  },
  {
    src: '/Images/brand-website-sample-journal.jpg',
    kind: 'Editorial · Journal article',
    alt: 'A laptop screen showing a company website blog article, a large headline, a byline line and a single column of body text below a wide header image.',
  },
  {
    src: '/Images/brand-website-sample-guide.jpg',
    kind: 'SEO content · Long-form guide',
    alt: 'A laptop screen showing a long-form guide page on a company website, a headline, a table-of-contents list on the left and a column of body text with subheadings on the right.',
  },
  {
    src: '/Images/brand-website-sample-geo.jpg',
    kind: 'GEO content · AI answer',
    alt: 'A phone held vertically, the screen showing a search results page with an AI-generated answer panel, a paragraph of answer text and a horizontal row of small cited source link chips beneath it.',
  },
];

const coverage = [
  {
    fig: 'B',
    label: 'Core pages',
    heading: 'Core pages. Who the company is.',
    image: '/Images/brand-website-core.jpg',
    alt: 'An open laptop on a studio desk, the screen filled with a company website homepage, a top navigation menu, a large headline, a short intro paragraph and a call-to-action button.',
    lead: 'The handful of pages a first-time visitor reads to figure out who you are: the homepage, the about page, the brand story.',
    body: [
      'Most company sites bury the one thing a new visitor came to find out. What does this company do, and why should I care? We write the core pages to answer that in the first screenful. The homepage leads with the offer instead of a tagline. The about page tells a real story, not a mission statement that trails off. The team and culture pages show actual people doing actual work.',
      'We settle the messaging order first: the single idea the site has to land, then the points that back it up. A creative director signs off on it. After that the same voice carries from the homepage all the way to the contact page, and the site finally holds together as one company.',
    ],
    tags: [
      'Homepage', 'About page', 'Brand story',
      'Mission and values', 'Team and culture', 'Messaging hierarchy',
      'Site navigation', 'Contact page',
    ],
  },
  {
    fig: 'C',
    label: 'Offerings',
    heading: 'Offerings. Products, services, solutions.',
    image: '/Images/brand-website-offerings.jpg',
    alt: 'An open laptop on a studio desk, the screen showing a company website service page, a headline, a block of explanatory text, a row of feature points and a call-to-action button.',
    lead: 'A clear page for everything you sell, written so the right buyer gets it fast.',
    body: [
      'Every product, service and solution gets its own page, and every page is built around the buyer instead of the org chart. What it is. Who it is for. The problem it takes off their plate. What working with you looks like. We write feature pages that explain rather than list, use-case and industry pages for buyers who only recognize their own situation, and comparison pages for the ones already shopping around.',
      'Three offerings or three hundred, each one gets the same depth and the same page shape, so a buyer can move between them without learning a new layout every time.',
    ],
    tags: [
      'Service pages', 'Product pages', 'Solution pages',
      'Use-case pages', 'Feature explainers', 'Comparison pages',
      'Industry pages', 'Buyer FAQs',
    ],
  },
  {
    fig: 'D',
    label: 'Editorial',
    heading: 'Editorial. The blog, the news, the journal.',
    image: '/Images/brand-website-editorial.jpg',
    alt: 'An open laptop on a studio desk, the screen showing a company website blog, an index of article cards each with a headline and a short summary, the top navigation menu visible above.',
    lead: 'The content that keeps the site alive after launch: the blog, the news, the think pieces, the resource library.',
    body: [
      'A site that never changes after launch tells visitors the company went quiet too. Editorial is what keeps it moving. Articles on a real calendar. Company news written so it is worth reading. Longer pieces that show how the company thinks. Case studies. A resource library that pulls in links and repeat visits.',
      'It runs off a published calendar, not off whoever happens to remember. We plan topics a quarter out, draft against the brief, and hold every piece to the standard of the homepage. The blog stops being an afterthought.',
    ],
    tags: [
      'Blog articles', 'News and press', 'Insight pieces',
      'Case studies', 'Resource library', 'Newsletters',
      'Editorial calendar', 'Thought leadership',
    ],
  },
  {
    fig: 'E',
    label: 'SEO content',
    heading: 'SEO content. Pages built to rank.',
    image: '/Images/brand-website-seo.jpg',
    alt: 'An open laptop on a studio desk, the screen showing a Google search results page, a list of blue link entries with titles and gray snippet text, a search box at the top.',
    lead: 'Pages and articles engineered to rank on Google, mapped to the terms people actually search and written for a person anyway.',
    body: [
      'We map the queries you should own, then build the pages to win them. Pillar pages on the core topics. Clusters of supporting articles linked underneath. Long guides that answer the question instead of circling it. The comparison and alternative pages buyers type in by name. Titles, meta descriptions, headings and internal links all get set on purpose, not handed off to a plugin.',
      'None of it reads like SEO, which is the whole point. The structure underneath is for the crawler. The words on top are for the reader, because Google spent a decade getting good at telling those two apart.',
    ],
    tags: [
      'Pillar pages', 'Topic clusters', 'Long-form guides',
      'Keyword landing pages', 'Titles and metadata', 'Internal linking',
      'Schema markup', 'Comparison pages',
    ],
  },
  {
    fig: 'F',
    label: 'GEO content',
    heading: 'GEO content. Quoted by the AI engines.',
    image: '/Images/brand-website-geo.jpg',
    alt: 'An open laptop on a studio desk, the screen showing a search results page with an AI-generated answer panel at the top, a block of answer text followed by a row of small cited source link chips.',
    lead: 'Content built to get quoted, not just ranked, so AI Overviews, ChatGPT and Perplexity name the brand in the answer.',
    body: [
      'Search is splitting into two halves. One still ends in a blue link. The other ends in an answer the reader takes and never clicks past. GEO content is written for that second half. We shape passages as clean, self-contained answers, add the FAQ and definition blocks the models tend to lift, publish numbers worth citing, and get the structured data and llms.txt in place so the crawlers read the site straight.',
      'The thing that decides it is the naming. The brand has to sit inside the quotable passage, not just somewhere on the page. Miss that and the engine paraphrases your answer and credits nobody. We write the passage so the credit lands with a name on it.',
    ],
    tags: [
      'Answer-led passages', 'FAQ blocks', 'llms.txt',
      'Citable statistics', 'Definitional content', 'Structured data',
      'Entity consistency', 'Quotable headings',
    ],
  },
];

const credentials = ['Private GPU infra', 'NDA & DPA', 'EU AI Act', 'Data sovereignty'];

const processSteps = [
  {
    short: 'Brief',
    tier: 'Where it is won',
    title: 'Discovery and brief.',
    body: 'One working session. We pin down what the site has to do, who it is for, and the one thing it has to make a visitor believe. You walk out with a one-page brief and a sitemap: every page the site needs, sorted by lane and tied to the moment in the visit it serves.',
    image: '/Images/brand-website-p1-brief.jpg',
    alt: 'A Chinese creative director and a Chinese brand lead at a studio table, a laptop between them showing a website sitemap diagram alongside printed page layouts.',
  },
  {
    short: 'Direction',
    tier: 'Where it is won',
    title: 'Editorial direction across the five lanes.',
    body: 'We set the voice, the messaging order and the content model: how a page is built, what each template has to carry, the keyword and query map behind the SEO and GEO lanes. References go up on the wall. The tone gets a name. We pick the AI models for each lane. This is the step that makes the site sound like you and nobody else.',
    image: '/Images/brand-website-p2-direction.jpg',
    alt: 'A storyboard wall with five columns of page sketches labeled Core pages, Offerings, Editorial, SEO and GEO, each column anchored by a printed mock of that page.',
  },
  {
    short: 'AIGC',
    tier: 'The rest is execution',
    title: 'AIGC drafting and authored finish.',
    body: 'Now the content gets made, lane by lane, page by page. AI drafts the bulk of it and helps with the design and the build. Then an editor rewrites every page against the brief and the voice. Nothing goes near the CMS until it has cleared the edit pass and the brand check.',
    image: '/Images/brand-website-p3-aigc.jpg',
    alt: 'A Chinese editor at a calibrated workstation, the main monitor showing a company website page draft in an editing view, the side panel showing a list of generated page variations.',
  },
  {
    short: 'Launch',
    tier: 'The rest is execution',
    title: 'Launch. Page by page, lane by lane.',
    body: 'Finished content goes into the CMS in the structure the site expects, with the metadata, headings, internal links and structured data already in place. Navigation and page order follow the sitemap from the brief. The site goes live organized, not as a heap of pages someone will tidy up later.',
    image: '/Images/brand-website-p4-launch.jpg',
    alt: 'A Chinese web producer at a daylit studio desk, the calibrated monitor showing a CMS dashboard with a list of website pages and a publish panel.',
  },
  {
    short: 'Adapt',
    tier: 'The rest is execution',
    title: 'Adapt. Refresh, expand, rerank.',
    body: 'A website is never really finished. New articles land on the editorial calendar. Core pages get refreshed as the company changes. The SEO and GEO lanes chase new queries as search itself moves. The brief still holds, though. You are adding to the site, not rebuilding it.',
    image: '/Images/brand-website-p5-adapt.jpg',
    alt: 'A Chinese editor at a calibrated desk, the main monitor showing a company website blog article open beside a search ranking panel, a smaller side monitor showing an editorial calendar.',
  },
];

const faqs = [
  {
    q: 'What exactly do you produce for a company website?',
    a: 'Five lanes of content, one studio. Core pages: the homepage, the about page and the brand story a first visit is built on. Offerings: a clear page for every product, service and solution. Editorial: the blog, the news, the think pieces and the resource library. SEO content: pages built to rank on Google. And GEO content: short passages written to get quoted by AI Overviews, ChatGPT and Perplexity. The five lanes share one editorial direction and one finishing pass, so you can hire a single lane or all of them and the site still reads in one voice.',
  },
  {
    q: 'How do you decide what the website should say and how to lay it out?',
    a: 'That is the first thing we do, before any writing starts. In the discovery session we work out what the site has to achieve, who it is for, and the one thing it has to make a visitor believe. Out of that comes the sitemap: every page the site needs, in what order, each one tied to a stage of the visit. Honestly, deciding what belongs on a company website, and in what order, is the hard part. The writing and the build get easier once the shape is right.',
  },
  {
    q: 'What is GEO content, and why does it matter now?',
    a: 'GEO stands for generative engine optimization: writing content so AI answer engines quote it. More and more searches now end inside an AI Overview, a ChatGPT reply or a Perplexity answer, where the reader gets the response and never clicks through to a site. GEO content is shaped as clean, self-contained passages those engines can lift, with the brand named inside the passage so the citation carries the company with it. SEO still earns you the ranking. GEO earns you a place in the answer that is starting to replace the ranking.',
  },
  {
    q: 'Do you design and build the site, or just write the content?',
    a: 'The content is ours to write: what each page says, how the site is laid out, every word of it. The design and the front-end build are AI-assisted and fast, and we hand the finished content into your CMS, Webflow, WordPress, a headless stack or Astro, structured and ready to publish. Our take is blunt. In 2026 the code and the layout are the cheap part. What goes on the site, and in what order, is what makes or breaks it.',
  },
  {
    q: 'How does pricing work?',
    a: 'We price by project, and usually by lane, so a company can start with one and add the rest later. What a project costs comes down to how many pages are in scope, how much editorial volume you want each month, and whether we are training a brand-voice model from scratch. Send a brief and a rough scope and a quote comes back, with no obligation to go ahead.',
  },
  {
    q: 'Can you produce content at the volume a full site and an active blog need?',
    a: 'Yes, and volume is where this model pays for itself. One site brief sets the master direction. After that, core pages, offering pages, articles and the SEO and GEO content all come off the same brief at a steady clip. The first page on a new brand is the expensive one. By the fiftieth, the cost per page is close to nothing, and every one of them still sounds the same.',
  },
  {
    q: 'How do you keep one voice across every page and every lane?',
    a: "The voice and the messaging order get set once, in the direction step, and a creative director signs off on them. From there, every page in every lane goes through the same editor and the same brand check. That shared finish is the reason a homepage, a buyer's solution page, a blog post and an SEO guide read like one company instead of five strangers who never compared notes.",
  },
  {
    q: 'What is your turnaround for a page or an article?',
    a: 'For a company we already know, a single page or article runs from brief to first draft in 24 to 72 hours. A new brand needs a one-time setup first, to lock the sitemap, the voice and the page templates. After that, a weekly drop of pages and articles at production quality is a comfortable pace.',
  },
  {
    q: 'How do you keep the writing from reading as AI-generated?',
    a: 'Every page is drafted with AI and then rewritten by an editor. We pull out the tells a reader senses even when they cannot name them: the hedging, the lists that always run to three, the openings that announce themselves, the words nobody says out loud. The draft is a starting point and nothing more. A person rewrites it against the brand voice before it ships, and that pass is the difference between copy a company can publish and copy a generator spat out.',
  },
  {
    q: 'Can you white-label this for our agency?',
    a: 'Yes. A good share of our website content work ships under agency brands, with full white-label delivery and a confidentiality agreement in place. The agency keeps the client relationship. We supply the editorial and production muscle behind it.',
  },
  {
    q: 'Who owns the content, the pages and the trained models?',
    a: 'You do. Every finished page, every article, every draft, and any custom model we train on your brand voice or your material, all of it belongs to you. Commercial rights for the AI outputs run through enterprise tiers that grant commercial use. The Copyright and AI guide covers the legal detail.',
    link: { match: 'Copyright and AI guide', href: '/resources/copyright-and-ai' },
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};
---

<Layout
  title="Brand Website Content: Pages, SEO & GEO at Scale"
  description="We write your company website: core pages, offerings, blog, SEO and GEO content. One brief, every page, made in a working studio."
>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

  <div class="prog" aria-hidden="true"><span class="prog__bar"></span></div>

  <div class="page">
    <!-- S1 · HERO -->
    <section class="hero">
      <div class="hero__panel">
        <svg class="hero__reg" viewBox="0 0 100 100" aria-hidden="true">
          <circle cx="50" cy="50" r="34" fill="none" stroke="currentColor" stroke-width="1.4" />
          <path d="M50 4v92M4 50h92" stroke="currentColor" stroke-width="1.4" />
        </svg>
        <div class="hero__panel-in">
          <p class="kicker kicker--light r">
            <span class="kicker__tick"></span>Platform
            <span class="kicker__sep">/</span> Brand website
          </p>
          <h1 class="hero__title">
            <span class="line"><span class="line__i">Five lanes,</span></span>
            <span class="line"><span class="line__i serif">one website brief.</span></span>
            <span class="line"><span class="line__i">Content, at scale.</span></span>
          </h1>
          <p class="hero__tag r" style="--d:.45s">
            Core pages. Offerings. Editorial. SEO. <em>GEO.</em>
          </p>
          <div class="hero__lead r" style="--d:.55s">
            <p>
              We write five kinds of content for a company website. Core pages
              that tell a visitor who you are. Offering pages for every
              product, service and solution you sell. Editorial for the blog
              and the press room. SEO pages built to rank on Google. And GEO
              content written to get the brand quoted by AI answer engines.
            </p>
            <p>
              All of it comes off a single brief. We work out what the site
              needs to say and how to lay it out, then we write every page.
              Design and code are the fast part now, mostly handled by AI.
              Content is the part we still write by hand, and it is what
              decides whether the site does its job.
            </p>
          </div>
          <div class="hero__cta r" style="--d:.65s">
            <Button variant="primary" href="/contact">Send the brief</Button>
            <a class="ghost-link ghost-link--light" href="#proof">
              See the work
              <svg viewBox="0 0 20 20" aria-hidden="true">
                <path d="M4 10h12M11 5l5 5-5 5" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            </a>
          </div>
          <p class="hero__meta r" style="--d:.72s">
            Hong Kong + Shanghai <span>/</span> website content under one editorial director
          </p>
        </div>
      </div>
      <div class="hero__visual">
        <img
          src="/Images/brand-website-hero.jpg"
          alt="A close editorial portrait of a hand holding a phone in warm rim light, the phone screen filled with a company website homepage, a top navigation menu, a large headline, a short paragraph of text and a call-to-action button visible."
          width="1024"
          height="1024"
          loading="eager"
          fetchpriority="high"
        />
        <span class="hero__chip">Platform work <span>/</span> Website</span>
      </div>
    </section>

    <div class="cbar" aria-hidden="true">
      {colourBar.map((c) => <span style={`background:${c}`}></span>)}
    </div>

    <!-- S2 · WHY -->
    <section class="why">
      <div class="shell">
        <div class="why__top">
          <div class="why__head">
            <p class="kicker kicker--light r"><span class="kicker__tick"></span>Why it matters</p>
            <h2 class="dh r" style="--d:.05s">
              What we ship<br /><span class="serif">into your website</span>.
            </h2>
          </div>
          <div class="why__text">
            <p class="r" style="--d:.1s">
              We handle five kinds of content for a company website. Core
              pages: the homepage, the about page, the story a first-time
              visitor reads to size you up. Offering pages: a clear page for
              each product, service and solution, written for the buyer.
              Editorial: the blog, the news, the longer think pieces, the
              resource library. SEO content: pages built to rank on Google.
              And GEO content: short, quotable passages written so AI
              Overviews, ChatGPT and Perplexity cite you by name.
            </p>
            <p class="r" style="--d:.16s">
              The work starts before anyone writes a sentence. First we settle
              the shape of the site: what each page has to say, and the order a
              visitor runs into it. Design and code are quick now, mostly AI.
              The writing is the part we own, and one editorial direction sits
              over all five lanes. So the homepage and the newest blog post
              sound like the same company.
            </p>
          </div>
        </div>

        <figure class="why__shot crop r-clip">
          <div class="crop__inner">
            <img
              src="/Images/brand-website-roots.jpg"
              alt="A wide editorial shot of two designers at a daylit studio table, four laptop screens lined up showing a company website homepage, an about page, a blog article and a search results page with an AI answer panel, one per content lane."
              width="1408"
              height="768"
              loading="lazy"
            />
          </div>
          <figcaption class="fig">Five lanes, one editorial direction</figcaption>
        </figure>

        <div class="stats">
          <p class="stats__quote r">
            One brief covers all five lanes. The same writers handle every
            page, from the homepage to the SEO library, and a first draft is
            back inside two days.
          </p>
          <div class="stats__grid">
            {
              stats.map((s, i) => (
                <div class="stat r" style={`--d:${i * 0.1}s`}>
                  <span
                    class="stat__num"
                    data-from={s.from}
                    data-to={s.to}
                    data-suffix={s.suffix}
                  >
                    {s.from}{s.suffix}
                  </span>
                  <span class="stat__label">{s.label}</span>
                  <span class="stat__note">{s.note}</span>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <!-- S3 · COVERAGE -->
    <section class="cover">
      <div class="shell">
        <header class="band">
          <p class="kicker r"><span class="kicker__tick"></span>The work, defined</p>
          <h2 class="dh r" style="--d:.05s">
            Five lanes, one <span class="serif">brand line</span>.
          </h2>
        </header>

        {
          coverage.map((c, i) => (
            <article class={`feature ${i % 2 === 1 ? 'feature--rev' : ''}`}>
              <figure class="feature__media crop r-clip">
                <div class="crop__inner">
                  <img
                    src={c.image}
                    alt={c.alt}
                    width="1024"
                    height="1024"
                    loading="lazy"
                  />
                </div>
                <figcaption class="fig">{c.label} lane</figcaption>
              </figure>
              <div class="feature__body">
                <p class="feature__idx r"><span class="feature__tick"></span>Lane</p>
                <h3 class="dh dh--sm r" style="--d:.05s">{c.heading}</h3>
                {c.lead && <p class="feature__lead r" style="--d:.1s">{c.lead}</p>}
                {c.body.map((para, pi) => (
                  <p class="feature__p r" style={`--d:${0.12 + pi * 0.06}s`}>
                    {para}
                  </p>
                ))}
                <p class="feature__scope r" style="--d:.24s">In scope</p>
                <ul class="chips r" style="--d:.28s">
                  {c.tags.map((t) => (
                    <li class="chip">{t}</li>
                  ))}
                </ul>
              </div>
            </article>
          ))
        }
      </div>
    </section>

    <!-- S4 · PROOF -->
    <section class="proof" id="proof">
      <div class="shell">
        <header class="band band--split">
          <div>
            <p class="kicker r"><span class="kicker__tick"></span>Contact sheet</p>
            <h2 class="dh r" style="--d:.05s">
              Selected <span class="serif">frames</span>.
            </h2>
          </div>
          <p class="band__note r" style="--d:.1s">
            A few frames from recent website work: homepage, article, search
            and answer-engine. Each one is written and built for the exact
            page it runs on, never stretched to fit.
          </p>
        </header>

        <div class="sheet">
          {
            samples.map((s, i) => (
              <figure class="shot crop r-clip" style={`--d:${(i % 3) * 0.08}s`}>
                <div class="crop__inner">
                  <img
                    src={s.src}
                    alt={s.alt}
                    width="1024"
                    height="1024"
                    loading="lazy"
                  />
                </div>
                <figcaption class="shot__cap">
                  <span class="shot__kind">{s.kind}</span>
                </figcaption>
              </figure>
            ))
          }
        </div>
      </div>
    </section>

    <!-- S5 · PARTNERSHIP -->
    <section class="source">
      <div class="shell source__grid">
        <div class="source__visual">
          <figure class="source__main crop">
            <div class="crop__inner">
              <img
                src="/Images/brand-website-hybrid-main.jpg"
                alt="A producer at a wide reference monitor showing a company website admin view, columns of page previews for the homepage, the offering pages, the blog and the SEO library arranged in a grid."
                width="1024"
                height="1024"
                loading="lazy"
              />
            </div>
          </figure>
          <figure class="source__inset">
            <img
              src="/Images/brand-website-hybrid-inset.jpg"
              alt="A close cinematic detail of a hand holding a phone vertically against a soft cream background, the screen filled with a company website homepage, a headline, a short paragraph and a call-to-action button."
              width="1024"
              height="1024"
              loading="lazy"
            />
          </figure>
          <div class="source__stat">
            <span class="source__stat-num">1 brief</span>
            <span class="source__stat-cap">Five lanes, every page, every market</span>
          </div>
        </div>

        <div class="source__body">
          <p class="kicker kicker--light r"><span class="kicker__tick"></span>Partnership</p>
          <h2 class="dh r" style="--d:.05s">
            Authored content, <span class="serif">AI volume</span>.
          </h2>
          <p class="source__lead r" style="--d:.1s">
            The company sites still gaining traffic in 2026 are not the ones
            with the slickest design. They are the ones that worked out what
            to say, and kept publishing it long after launch.
          </p>
          <p class="source__p r" style="--d:.16s">
            That is the gap we work in, between the editorial and the
            technical. Content strategists, editors and writers on one side.
            The AIGC pipeline, the SEO and GEO tooling, the CMS build on the
            other. Design and code move fast because AI does the heavy
            lifting. The writing is done by people, and the same people sign
            off on page one and page two hundred.
          </p>
          <ul class="creds r" style="--d:.22s">
            {
              credentials.map((c) => (
                <li class="cred">
                  <svg viewBox="0 0 14 14" aria-hidden="true">
                    <path d="M2 7.5 5.5 11 12 3.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                  </svg>
                  {c}
                </li>
              ))
            }
          </ul>
          <p class="source__p r" style="--d:.26s">
            It all runs on our own infrastructure. Hosting sits in mainland
            China, Hong Kong, the EU or the US, wherever your data rules say
            it has to. Any model we train on your brand voice is yours, and
            you can take it with you if we ever part ways.
          </p>
          <p class="source__p source__p--quiet r" style="--d:.3s">
            Some companies hand us the whole site. Others just want the SEO and
            GEO lanes. Both are fine by us.
          </p>
        </div>
      </div>
    </section>

    <!-- S6 · PROCESS -->
    <section class="craft" id="craft">
      <div class="shell craft__intro">
        <p class="kicker r"><span class="kicker__tick"></span>How a website build runs</p>
        <h2 class="dh r" style="--d:.05s">
          One brief,<br />every <span class="serif">page</span>.
        </h2>
        <p class="craft__lede r" style="--d:.12s">
          All five lanes share one editorial direction and one process. The
          lane changes the format. It does not change how the work gets made.
          And the calls that matter most get made early, well before launch
          day.
        </p>
        <p class="craft__note r" style="--d:.16s">
          The first two steps decide the site. Everything after is execution.
        </p>
      </div>

      <div class="seq" id="seq">
        <div class="seq__stick">
          <div class="shell seq__stage">
            <div class="seq__nav" aria-label="Process steps">
              {
                processSteps.map((s, i) => (
                  <button
                    class="seq__step"
                    type="button"
                    data-i={i}
                    data-active={i === 0 ? 'true' : 'false'}
                  >
                    <span class="seq__step-dot">
                      <span class="seq__step-fill"></span>
                    </span>
                    <span class="seq__step-label">{s.short}</span>
                  </button>
                ))
              }
            </div>

            <div class="seq__panels">
              {
                processSteps.map((s, i) => (
                  <article
                    class="seq__panel"
                    data-panel={i}
                    data-active={i === 0 ? 'true' : 'false'}
                  >
                    <figure class="seq__media crop">
                      <div class="crop__inner">
                        <img
                          src={s.image}
                          alt={s.alt}
                          width="1200"
                          height="900"
                          loading="lazy"
                        />
                      </div>
                    </figure>
                    <div class="seq__text">
                      <span class="seq__tier">{s.tier}</span>
                      <h3 class="seq__title">{s.title}</h3>
                      <p class="seq__body">{s.body}</p>
                    </div>
                  </article>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- S7 · FAQ -->
    <section class="faq">
      <div class="shell faq__grid">
        <aside class="faq__intro">
          <p class="kicker r"><span class="kicker__tick"></span>Questions</p>
          <h2 class="dh r" style="--d:.05s">
            Before the<br />first <span class="serif">call</span>.
          </h2>
          <p class="faq__sub r" style="--d:.1s">
            The questions companies ask before we start. If yours is not here,
            put it in the brief.
          </p>
          <div class="r" style="--d:.16s">
            <Button variant="primary" href="/contact">Send the brief</Button>
          </div>
          <p class="fig r" style="--d:.2s">Reviewed May 2026</p>
        </aside>

        <div class="acc">
          {
            faqs.map((faq) => (
              <div class="acc__row r">
                <button class="acc__q" type="button" aria-expanded="false">
                  <span class="acc__qmark">Q</span>
                  <span class="acc__qtext">{faq.q}</span>
                  <span class="acc__icon" aria-hidden="true">
                    <span></span><span></span>
                  </span>
                </button>
                <div class="acc__a">
                  <div class="acc__a-inner">
                    <p>
                      {faq.link ? (
                        <>
                          {faq.a.split(faq.link.match)[0]}
                          <a href={faq.link.href}>{faq.link.match}</a>
                          {faq.a.split(faq.link.match)[1]}
                        </>
                      ) : (
                        faq.a
                      )}
                    </p>
                  </div>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- S8 · CLOSE -->
    <section class="end">
      <svg class="end__reg" viewBox="0 0 100 100" aria-hidden="true">
        <circle cx="50" cy="50" r="34" fill="none" stroke="currentColor" stroke-width="1.4" />
        <path d="M50 4v92M4 50h92" stroke="currentColor" stroke-width="1.4" />
      </svg>
      <div class="shell end__inner">
        <p class="kicker kicker--light r"><span class="kicker__tick"></span>Try us</p>
        <h2 class="end__title r" style="--d:.05s">
          Send the website brief.<br />Back in <span class="serif">48 hours</span>.
        </h2>
        <p class="end__p r" style="--d:.12s">
          You get a sitemap recommendation, a scope broken down by lane, a
          rough timeline, and a couple of sample pages written for free, so
          you can see how we think before any budget is on the table.
        </p>
        <div class="end__cta r" style="--d:.18s">
          <Button variant="primary" href="/contact">Send the brief</Button>
          <a class="ghost-link ghost-link--light" href="/work">
            See more work
            <svg viewBox="0 0 20 20" aria-hidden="true">
              <path d="M4 10h12M11 5l5 5-5 5" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </a>
        </div>
      </div>
      <div class="cbar cbar--end" aria-hidden="true">
        {colourBar.map((c) => <span style={`background:${c}`}></span>)}
      </div>
    </section>
  </div>
</Layout>

<style>
  .page {
    --ink: #0e1633;
    --navy: #0e1f4e;
    --paper: #f0ede9;
    --paper-2: #e7e3dd;
    overflow: clip;
  }
  .shell {
    width: 100%;
    max-width: 1280px;
    margin-inline: auto;
    padding-inline: clamp(22px, 5vw, 60px);
  }

  .prog { position: fixed; inset: 0 0 auto 0; height: 3px; z-index: 200; pointer-events: none; }
  .prog__bar { display: block; height: 100%; width: 100%; transform: scaleX(0); transform-origin: 0 50%; background: var(--brand-orange); }

  .r, .r-clip { --d: 0s; }
  .r { opacity: 0; transform: translateY(26px); transition: opacity 0.7s ease, transform 0.8s cubic-bezier(0.16, 1, 0.3, 1); transition-delay: var(--d); }
  .r.in { opacity: 1; transform: none; }
  .r-clip .crop__inner { clip-path: inset(0 0 101% 0); transition: clip-path 1.05s cubic-bezier(0.16, 1, 0.3, 1); transition-delay: var(--d, 0s); }
  .r-clip .crop__inner img { transform: scale(1.12); transition: transform 1.3s cubic-bezier(0.16, 1, 0.3, 1); transition-delay: var(--d, 0s); }
  .r-clip.in .crop__inner { clip-path: inset(0 0 0 0); }
  .r-clip.in .crop__inner img { transform: scale(1); }
  @media (prefers-reduced-motion: reduce) {
    .r, .r.in { opacity: 1; transform: none; transition: none; }
    .r-clip .crop__inner, .r-clip .crop__inner img { clip-path: none; transform: none; transition: none; }
  }

  .kicker { display: flex; align-items: center; gap: 9px; font-family: var(--font-mono); font-size: 11px; font-weight: 500; letter-spacing: 0.16em; text-transform: uppercase; color: var(--text-mute); }
  .kicker--light { color: rgba(240, 237, 233, 0.66); }
  .kicker__tick { width: 22px; height: 0; border-top: 1.5px solid var(--brand-orange); }
  .kicker__sep { color: var(--brand-orange); }

  .dh { font-family: var(--font-sans); font-weight: 600; font-size: clamp(34px, 4.4vw, 62px); line-height: 1.02; letter-spacing: -0.03em; }
  .dh--sm { font-size: clamp(26px, 2.6vw, 38px); }
  .serif { font-family: var(--font-serif); font-style: italic; font-weight: 500; letter-spacing: -0.01em; color: var(--brand-orange); }

  .crop { position: relative; margin: 0; }
  .crop::before, .crop::after { content: ''; position: absolute; width: 16px; height: 16px; z-index: 2; }
  .crop::before { top: -9px; left: -9px; border-top: 1.5px solid var(--brand-orange); border-left: 1.5px solid var(--brand-orange); }
  .crop::after { right: -9px; bottom: 28px; border-right: 1.5px solid var(--brand-orange); border-bottom: 1.5px solid var(--brand-orange); }
  .crop__inner { overflow: hidden; border-radius: 4px; }
  .crop__inner img { display: block; width: 100%; height: 100%; object-fit: cover; }
  .fig { margin-top: 14px; font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-mute); }

  .ghost-link { display: inline-flex; align-items: center; gap: 9px; font-size: var(--type-small); font-weight: 600; color: var(--text-ink); }
  .ghost-link svg { width: 19px; height: 19px; transition: transform 0.24s ease-out; }
  .ghost-link:hover svg { transform: translateX(5px); }
  .ghost-link--light { color: var(--text-on-dark); }

  .band { max-width: 720px; margin-bottom: clamp(40px, 5vw, 70px); display: grid; gap: 18px; }
  .band--split { max-width: none; grid-template-columns: 1fr; }
  @media (min-width: 880px) {
    .band--split { grid-template-columns: 1fr 0.82fr; align-items: end; gap: 18px 60px; }
    .band--split > div { display: grid; gap: 18px; }
  }
  .band__note { font-size: var(--type-lead); line-height: 1.5; color: var(--text-mute); }

  .cbar { display: flex; height: 12px; }
  .cbar span { flex: 1; }
  .cbar--end { margin-top: clamp(60px, 8vw, 110px); }

  /* Hero */
  .hero { display: grid; background: var(--ink); color: var(--text-on-dark); }
  @media (min-width: 900px) { .hero { grid-template-columns: 1fr 1fr; } }
  .hero__panel { position: relative; display: flex; align-items: center; padding: clamp(40px, 4.4vw, 72px) clamp(24px, 5vw, 64px); overflow: clip; }
  .hero__reg { position: absolute; width: clamp(240px, 28vw, 420px); right: -12%; bottom: -14%; color: rgba(240, 237, 233, 0.055); pointer-events: none; }
  .hero__panel-in { position: relative; width: 100%; max-width: 600px; }
  .hero__title { margin-top: 24px; font-family: var(--font-sans); font-weight: 600; font-size: clamp(46px, 6.6vw, 92px); line-height: 1.02; letter-spacing: -0.045em; }
  .hero__title .line { display: block; overflow: hidden; padding: 0.22em 0.14em; margin: -0.22em -0.14em; }
  .hero__title .line__i { display: block; transform: translateY(145%); transition: transform 0.9s cubic-bezier(0.16, 1, 0.3, 1); }
  .hero__title .line:nth-child(1) .line__i { transition-delay: 0.12s; }
  .hero__title .line:nth-child(2) .line__i { transition-delay: 0.22s; }
  .hero__title .line:nth-child(3) .line__i { transition-delay: 0.32s; }
  .hero.in .hero__title .line__i { transform: translateY(0); }
  .hero__title .serif { font-size: 1.04em; }
  @media (prefers-reduced-motion: reduce) { .hero__title .line__i { transform: none; } }
  .hero__tag { margin-top: 26px; font-size: clamp(18px, 1.5vw, 23px); font-weight: 600; letter-spacing: -0.02em; }
  .hero__tag em { font-family: var(--font-serif); font-style: italic; font-weight: 500; color: var(--brand-orange); }
  .hero__lead { margin-top: 20px; display: grid; gap: 13px; max-width: 46ch; }
  .hero__lead p { font-size: var(--type-body); line-height: 1.62; color: rgba(240, 237, 233, 0.74); }
  .hero__cta { margin-top: 30px; display: flex; flex-wrap: wrap; align-items: center; gap: 14px 26px; }
  .hero__meta { margin-top: 34px; padding-top: 20px; border-top: 1.5px dashed rgba(240, 237, 233, 0.2); font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(240, 237, 233, 0.5); }
  .hero__meta span { color: var(--brand-orange); }
  .hero__visual { position: relative; overflow: hidden; min-height: 58vh; }
  @media (min-width: 900px) { .hero__visual { min-height: auto; } }
  .hero__visual img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; transform: scale(1.08); transition: transform 1.5s cubic-bezier(0.16, 1, 0.3, 1); }
  .hero.in .hero__visual img { transform: scale(1); }
  @media (prefers-reduced-motion: reduce) { .hero__visual img { transform: none; } }
  .hero__chip { position: absolute; left: 18px; bottom: 18px; z-index: 2; display: inline-flex; gap: 8px; padding: 9px 15px; border-radius: var(--radius-pill); background: rgba(14, 22, 51, 0.78); font-family: var(--font-mono); font-size: 10px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-on-dark); }
  .hero__chip span { color: var(--brand-orange); }

  /* Why */
  .why { background: var(--ink); color: var(--text-on-dark); padding-block: clamp(80px, 10vw, 150px); }
  .why__top { display: grid; gap: 30px; }
  @media (min-width: 900px) { .why__top { grid-template-columns: 1fr 1fr; gap: 70px; align-items: start; } }
  .why__head .dh { margin-top: 18px; }
  .why__text { display: grid; gap: 16px; align-content: start; padding-top: 6px; }
  .why__text p { font-size: var(--type-body); line-height: 1.62; color: rgba(240, 237, 233, 0.78); }
  .why__clients { color: rgba(240, 237, 233, 0.6) !important; }
  .why__clients strong { color: var(--text-on-dark); font-weight: 600; }
  .why__shot { margin-top: clamp(40px, 5vw, 72px); }
  .why__shot .crop__inner { aspect-ratio: 16 / 8; }
  .why__shot .fig { color: rgba(240, 237, 233, 0.5); }

  .stats { margin-top: clamp(48px, 6vw, 84px); border-top: 1.5px dashed rgba(240, 237, 233, 0.22); padding-top: clamp(36px, 4vw, 56px); }
  .stats__quote { max-width: 60ch; font-family: var(--font-serif); font-style: italic; font-size: clamp(19px, 1.9vw, 25px); line-height: 1.42; color: var(--text-on-dark); }
  .stats__grid { margin-top: 40px; display: grid; gap: 30px; }
  @media (min-width: 720px) { .stats__grid { grid-template-columns: repeat(3, 1fr); gap: 24px; } }
  .stat { display: grid; gap: 5px; padding-top: 24px; border-top: 1.5px solid rgba(240, 237, 233, 0.16); }
  .stat__num { font-size: clamp(56px, 7vw, 96px); font-weight: 700; line-height: 1; letter-spacing: -0.04em; color: var(--brand-orange); }
  .stat__label { margin-top: 6px; font-size: 16px; font-weight: 600; }
  .stat__note { font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.05em; text-transform: uppercase; color: rgba(240, 237, 233, 0.55); }

  /* Coverage */
  .cover { background: var(--paper); padding-block: clamp(80px, 10vw, 150px); }
  .feature { display: grid; gap: 36px; align-items: center; padding-block: clamp(40px, 5vw, 72px); border-top: 1.5px dashed var(--line-strong); }
  .feature:last-child { border-bottom: 1.5px dashed var(--line-strong); }
  @media (min-width: 900px) {
    .feature { grid-template-columns: 1fr 1fr; gap: 72px; }
    .feature--rev .feature__media { order: 2; }
  }
  .feature__media .crop__inner { aspect-ratio: 5 / 4; }
  .feature__idx { display: flex; align-items: center; gap: 12px; font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.16em; text-transform: uppercase; color: var(--text-mute); }
  .feature__tick { width: 22px; height: 0; border-top: 1.5px solid var(--brand-orange); }
  .feature__body .dh { margin-top: 16px; }
  .feature__lead { margin-top: 14px; font-size: var(--type-lead); line-height: 1.42; color: var(--text-ink); font-weight: 500; }
  .feature__p { margin-top: 14px; font-size: var(--type-body); line-height: 1.66; color: var(--text-mute); }
  .feature__scope { margin-top: 24px; padding-top: 18px; border-top: 1px solid var(--line-hairline); font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--text-mute); }
  .chips { margin-top: 14px; display: flex; flex-wrap: wrap; gap: 8px; }
  .chip { padding: 8px 14px; border: 1px solid var(--line-strong); border-radius: var(--radius-pill); font-size: 12px; font-weight: 500; transition: background-color 0.18s ease, color 0.18s ease, border-color 0.18s ease; }
  .chip:hover { background: var(--brand-orange); border-color: var(--brand-orange); color: #fff; }

  /* Proof */
  .proof { background: var(--bg-white); padding-block: clamp(80px, 10vw, 150px); }
  .sheet { display: grid; gap: clamp(22px, 3vw, 40px); }
  @media (min-width: 720px) {
    .sheet { grid-template-columns: repeat(3, 1fr); }
    .sheet .shot:nth-child(3n + 2) { margin-top: 60px; }
    .sheet .shot:nth-child(3n) { margin-top: 30px; }
  }
  .shot .crop__inner { aspect-ratio: 1 / 1; }
  .shot .crop__inner img { transition: transform 0.7s cubic-bezier(0.16, 1, 0.3, 1); }
  .shot:hover .crop__inner img { transform: scale(1.06); }
  .shot__cap { margin-top: 14px; display: flex; align-items: baseline; padding-bottom: 10px; border-bottom: 1.5px solid var(--ink); }
  .shot__kind { font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-ink); }

  /* Partnership / Source */
  .source { background: var(--navy); color: var(--text-on-dark); padding-block: clamp(80px, 10vw, 150px); }
  .source__grid { display: grid; gap: 52px; }
  @media (min-width: 940px) { .source__grid { grid-template-columns: 0.92fr 1.08fr; gap: 70px; align-items: center; } }
  .source__visual { position: relative; }
  .source__main .crop__inner { aspect-ratio: 5 / 4; }
  .source__inset { position: absolute; right: -14px; bottom: -34px; width: 42%; max-width: 200px; margin: 0; border-radius: 8px; overflow: hidden; border: 5px solid var(--navy); box-shadow: 0 20px 36px rgba(0, 0, 0, 0.42); }
  .source__inset img { display: block; width: 100%; aspect-ratio: 1 / 1; object-fit: cover; }
  @media (max-width: 520px) { .source__inset { display: none; } }
  .source__stat { position: absolute; left: -10px; top: 36px; display: grid; gap: 3px; padding: 15px 19px; border-radius: 12px; background: var(--brand-orange); color: #fff; box-shadow: 0 16px 32px rgba(233, 78, 27, 0.42); }
  .source__stat-num { font-size: 24px; font-weight: 700; letter-spacing: -0.02em; }
  .source__stat-cap { font-family: var(--font-mono); font-size: 9px; letter-spacing: 0.05em; text-transform: uppercase; max-width: 15ch; line-height: 1.4; }
  .source__body .dh { margin-top: 16px; }
  .source__lead { margin-top: 20px; font-size: var(--type-lead); line-height: 1.5; color: var(--text-on-dark); }
  .source__p { margin-top: 15px; font-size: var(--type-body); line-height: 1.66; color: rgba(240, 237, 233, 0.78); }
  .source__p--quiet { color: rgba(240, 237, 233, 0.55); }
  .creds { margin-top: 22px; display: flex; flex-wrap: wrap; gap: 9px; }
  .cred { display: inline-flex; align-items: center; gap: 8px; padding: 9px 15px; border-radius: var(--radius-pill); border: 1px solid rgba(240, 237, 233, 0.2); font-family: var(--font-mono); font-size: 12px; font-weight: 500; transition: border-color 0.2s ease, background-color 0.2s ease; }
  .cred:hover { border-color: var(--brand-orange); background: rgba(233, 78, 27, 0.16); }
  .cred svg { width: 13px; height: 13px; color: var(--brand-orange); }

  /* Process */
  .craft { background: var(--paper); padding-top: clamp(80px, 10vw, 150px); }
  .craft__intro { max-width: 780px; margin-bottom: clamp(36px, 5vw, 60px); }
  .craft__intro .kicker { color: var(--brand-orange); }
  .craft__intro .dh { margin-top: 16px; }
  .craft__lede { margin-top: 18px; font-size: var(--type-lead); line-height: 1.5; color: var(--text-mute); }
  .craft__note { margin-top: 22px; display: inline-block; padding: 13px 20px; border-left: 3px solid var(--brand-orange); background: rgba(233, 78, 27, 0.07); border-radius: 0 10px 10px 0; font-family: var(--font-mono); font-size: 12px; letter-spacing: 0.04em; color: var(--text-ink); }

  .seq__nav { display: none; }
  .seq__panels { display: grid; gap: clamp(48px, 7vw, 96px); padding-bottom: clamp(80px, 10vw, 150px); }
  .seq__panel { display: grid; gap: 28px; }
  @media (min-width: 760px) {
    .seq__panel { grid-template-columns: 1fr 1fr; gap: 56px; align-items: center; }
    .seq__panel:nth-child(even) .seq__media { order: 2; }
  }
  .seq__media .crop__inner { aspect-ratio: 4 / 3; }
  .seq__tier { font-family: var(--font-mono); font-size: 11px; font-weight: 500; letter-spacing: 0.14em; text-transform: uppercase; color: var(--brand-orange); }
  .seq__title { margin-top: 12px; font-size: clamp(25px, 2.7vw, 38px); font-weight: 600; line-height: 1.12; letter-spacing: -0.025em; }
  .seq__body { margin-top: 14px; font-size: var(--type-body); line-height: 1.66; color: var(--text-mute); }

  .craft--pinned .seq { height: 480vh; }
  .craft--pinned .seq__stick { position: sticky; top: 0; height: 100vh; display: flex; align-items: center; overflow: hidden; }
  .craft--pinned .seq__stage { width: 100%; display: flex; flex-direction: column; gap: clamp(22px, 3vw, 40px); }
  .craft--pinned .seq__nav { display: grid; grid-template-columns: repeat(5, 1fr); gap: 10px; }
  .seq__step { display: flex; flex-direction: column; gap: 10px; padding: 0; background: transparent; border: none; text-align: left; cursor: pointer; }
  .seq__step-dot { height: 5px; border-radius: 3px; background: var(--line-strong); overflow: hidden; }
  .seq__step-fill { display: block; height: 100%; width: 100%; background: var(--brand-orange); transform: scaleX(0); transform-origin: 0 50%; }
  .seq__step-label { font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.07em; text-transform: uppercase; color: var(--text-mute); transition: color 0.3s ease; }
  .seq__step[data-active='true'] .seq__step-label { color: var(--text-ink); }
  .craft--pinned .seq__panels { position: relative; display: block; height: clamp(340px, 60vh, 540px); padding-bottom: 0; }
  .craft--pinned .seq__panel { position: absolute; inset: 0; align-content: center; opacity: 0; visibility: hidden; transform: translateY(26px); transition: opacity 0.5s ease, transform 0.6s cubic-bezier(0.16, 1, 0.3, 1), visibility 0s linear 0.5s; }
  .craft--pinned .seq__panel[data-active='true'] { opacity: 1; visibility: visible; transform: none; transition: opacity 0.5s ease, transform 0.6s cubic-bezier(0.16, 1, 0.3, 1); }
  .craft--pinned .seq__panel:nth-child(even) .seq__media { order: 0; }
  .craft--pinned .seq__media .crop__inner { aspect-ratio: auto; height: clamp(280px, 52vh, 460px); }
  @media (prefers-reduced-motion: reduce) { .craft--pinned .seq__panel { transition: none; } }

  /* FAQ */
  .faq { background: var(--paper); padding-block: clamp(80px, 10vw, 150px); }
  .faq__grid { display: grid; gap: 44px; }
  @media (min-width: 1000px) {
    .faq__grid { grid-template-columns: 0.78fr 1.22fr; gap: 72px; align-items: start; }
    .faq__intro { position: sticky; top: 96px; }
  }
  .faq__intro .dh { margin-top: 18px; }
  .faq__sub { margin-top: 18px; margin-bottom: 26px; font-size: var(--type-body); line-height: 1.62; color: var(--text-mute); }
  .acc { display: grid; }
  .acc__row { border-top: 1.5px solid var(--ink); }
  .acc__row:last-child { border-bottom: 1.5px solid var(--ink); }
  .acc__q { width: 100%; display: grid; grid-template-columns: auto 1fr auto; align-items: start; gap: 16px; padding: 26px 0; background: transparent; border: none; text-align: left; }
  .acc__qmark { font-family: var(--font-mono); font-size: 13px; font-weight: 600; color: var(--brand-orange); padding-top: 3px; }
  .acc__qtext { font-size: clamp(17px, 1.6vw, 21px); font-weight: 600; letter-spacing: -0.01em; color: var(--text-ink); transition: color 0.18s ease; }
  .acc__q:hover .acc__qtext { color: var(--brand-orange); }
  .acc__icon { position: relative; width: 22px; height: 22px; margin-top: 2px; }
  .acc__icon span { position: absolute; top: 50%; left: 50%; width: 14px; height: 2px; border-radius: 2px; background: var(--text-ink); transform: translate(-50%, -50%); transition: transform 0.28s cubic-bezier(0.16, 1, 0.3, 1); }
  .acc__icon span:last-child { transform: translate(-50%, -50%) rotate(90deg); }
  .acc__q[aria-expanded='true'] .acc__icon span:last-child { transform: translate(-50%, -50%) rotate(0deg); }
  .acc__q[aria-expanded='true'] .acc__icon span { background: var(--brand-orange); }
  .acc__a { display: grid; grid-template-rows: 0fr; transition: grid-template-rows 0.32s ease; }
  .acc__q[aria-expanded='true'] + .acc__a { grid-template-rows: 1fr; }
  .acc__a-inner { overflow: hidden; }
  .acc__a-inner p { padding: 0 0 26px 41px; max-width: 64ch; font-size: var(--type-body); line-height: 1.66; color: var(--text-mute); }
  .acc__a-inner a { color: var(--brand-orange); font-weight: 600; text-decoration: underline; text-underline-offset: 2px; }

  /* Close */
  .end { position: relative; background: var(--ink); color: var(--text-on-dark); padding-top: clamp(80px, 11vw, 150px); overflow: clip; }
  .end__reg { position: absolute; width: clamp(300px, 36vw, 560px); top: -16%; right: -9%; color: rgba(240, 237, 233, 0.05); pointer-events: none; }
  .end__inner { position: relative; display: grid; justify-items: start; gap: 22px; }
  .end__title { font-family: var(--font-sans); font-weight: 600; font-size: clamp(40px, 6.6vw, 96px); line-height: 0.98; letter-spacing: -0.04em; }
  .end__p { max-width: 54ch; font-size: var(--type-lead); line-height: 1.5; color: rgba(240, 237, 233, 0.78); }
  .end__cta { margin-top: 8px; display: flex; flex-wrap: wrap; align-items: center; gap: 14px 26px; }
</style>

<script>
  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');

  const progBar = document.querySelector<HTMLElement>('.prog__bar');
  if (progBar) {
    const onScroll = () => {
      const d = document.documentElement;
      const max = d.scrollHeight - d.clientHeight;
      progBar.style.transform = `scaleX(${max > 0 ? Math.min(1, d.scrollTop / max) : 0})`;
    };
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', onScroll);
  }

  const revealEls = document.querySelectorAll('.r, .r-clip, .hero');
  if ('IntersectionObserver' in window) {
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            e.target.classList.add('in');
            io.unobserve(e.target);
          }
        });
      },
      { threshold: 0.16, rootMargin: '0px 0px -7% 0px' },
    );
    revealEls.forEach((el) => io.observe(el));
  } else {
    revealEls.forEach((el) => el.classList.add('in'));
  }

  const nums = Array.from(document.querySelectorAll<HTMLElement>('.stat__num'));
  const runCount = (el: HTMLElement) => {
    const from = Number(el.dataset.from || 0);
    const to = Number(el.dataset.to || 0);
    const suffix = el.dataset.suffix || '';
    if (reduceMotion.matches) {
      el.textContent = `${to}${suffix}`;
      return;
    }
    const dur = 1500;
    const t0 = performance.now();
    const tick = (now: number) => {
      const p = Math.min(1, (now - t0) / dur);
      const eased = 1 - Math.pow(1 - p, 3);
      el.textContent = `${Math.round(from + (to - from) * eased)}${suffix}`;
      if (p < 1) requestAnimationFrame(tick);
    };
    requestAnimationFrame(tick);
  };
  if ('IntersectionObserver' in window && nums.length) {
    const io2 = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            runCount(e.target as HTMLElement);
            io2.unobserve(e.target);
          }
        });
      },
      { threshold: 0.5 },
    );
    nums.forEach((n) => io2.observe(n));
  } else {
    nums.forEach(runCount);
  }

  const craft = document.getElementById('craft');
  const seq = document.getElementById('seq');
  if (craft && seq) {
    const steps = Array.from(seq.querySelectorAll<HTMLElement>('.seq__step'));
    const panels = Array.from(seq.querySelectorAll<HTMLElement>('.seq__panel'));
    const fills = steps.map((s) => s.querySelector<HTMLElement>('.seq__step-fill'));
    const n = panels.length;
    const wide = window.matchMedia('(min-width: 880px)');

    const setActive = (idx: number, sub: number) => {
      panels.forEach((p, i) => (p.dataset.active = String(i === idx)));
      steps.forEach((s, i) => {
        s.dataset.active = String(i === idx);
        const f = fills[i];
        if (f) f.style.transform = `scaleX(${i < idx ? 1 : i === idx ? sub : 0})`;
      });
    };

    let ticking = false;
    const update = () => {
      ticking = false;
      if (!craft.classList.contains('craft--pinned')) return;
      const total = seq.offsetHeight - window.innerHeight;
      const scrolled = Math.min(total, Math.max(0, -seq.getBoundingClientRect().top));
      const p = total > 0 ? scrolled / total : 0;
      const exact = p * n;
      const idx = Math.min(n - 1, Math.floor(exact));
      const sub = Math.min(1, Math.max(0, exact - idx));
      setActive(idx, idx === n - 1 ? 1 : sub);
    };
    const onScrollSeq = () => {
      if (!ticking) {
        ticking = true;
        requestAnimationFrame(update);
      }
    };

    const applyMode = () => {
      if (wide.matches && !reduceMotion.matches) {
        craft.classList.add('craft--pinned');
        update();
      } else {
        craft.classList.remove('craft--pinned');
        panels.forEach((p) => (p.dataset.active = 'true'));
      }
    };

    steps.forEach((s, i) => {
      s.addEventListener('click', () => {
        if (!craft.classList.contains('craft--pinned')) return;
        const railTop = seq.getBoundingClientRect().top + window.scrollY;
        const total = seq.offsetHeight - window.innerHeight;
        window.scrollTo({
          top: railTop + ((i + 0.5) / n) * total,
          behavior: reduceMotion.matches ? 'auto' : 'smooth',
        });
      });
    });

    window.addEventListener('scroll', onScrollSeq, { passive: true });
    window.addEventListener('resize', () => {
      applyMode();
      update();
    });
    applyMode();
  }

  const accQs = Array.from(document.querySelectorAll<HTMLButtonElement>('.acc__q'));
  accQs.forEach((q) => {
    q.addEventListener('click', () => {
      const open = q.getAttribute('aria-expanded') === 'true';
      accQs.forEach((o) => o.setAttribute('aria-expanded', 'false'));
      q.setAttribute('aria-expanded', open ? 'false' : 'true');
    });
  });
</script>
